[project]
name = "goa-map-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "drawsvg>=2.4.0",
    "fastapi[all]>=0.124.0",
    "osmnx>=2.0.7",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
